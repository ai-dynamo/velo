# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: CI

on:
  push:
    branches:
    - ops-4105/ci-runners # todo: remove this after testing
    - main
  pull_request:

# concurrency:
#   group: ${{ github.event_name == 'pull_request' && format('{0}-{1}', github.workflow, github.event.pull_request.number) || format('{0}-{1}', github.workflow, github.run_id) }}
#   cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  CARGO_TERM_COLOR: always
  PROTOC_VERSION: "30.2"

jobs:
  fmt:
    name: Formatting
    runs-on: stg-velo-default-v1
    container:
      image: ${{ vars.RUNNERS_REGISTRY }}/dynamo-ci/aws/dynamo/actions-runner/velo/default:0.0.2
    permissions:
      contents: read
    steps:
    - uses: actions/checkout@v4
    # - name: Add rustfmt component
    #   run: rustup component add rustfmt
    # - name: Check formatting
    #   run: cargo fmt --check

  # clippy:
  #   name: Clippy
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #   steps:
  #   - uses: actions/checkout@v4
  #   - name: Install protoc
  #     run: |
  #       PB_REL="https://github.com/protocolbuffers/protobuf/releases"
  #       PROTOC_ZIP="protoc-${PROTOC_VERSION}-linux-x86_64.zip"
  #       curl -fsSLO "$PB_REL/download/v${PROTOC_VERSION}/${PROTOC_ZIP}"
  #       unzip "${PROTOC_ZIP}" -d $HOME/.local
  #       rm "${PROTOC_ZIP}"
  #       echo "$HOME/.local/bin" >> $GITHUB_PATH
  #   - name: Install system dependencies
  #     run: sudo apt-get update -qq && sudo apt-get install -y -qq cmake
  #   - name: Cache cargo registry and tools
  #     uses: actions/cache@v4
  #     with:
  #       path: |
  #         ~/.cargo/bin/
  #         ~/.cargo/registry/
  #         ~/.cargo/git/
  #       key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
  #       restore-keys: ${{ runner.os }}-cargo-registry-
  #   - name: Add clippy component
  #     run: rustup component add clippy
  #   - name: Run clippy
  #     run: cargo clippy --no-deps --all-features --all-targets -- -D warnings
  #   - name: Install and run cargo-machete
  #     run: |
  #       cargo install cargo-machete --locked || true
  #       cargo machete

  # deny:
  #   name: Cargo Deny
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #   steps:
  #   - uses: actions/checkout@v4
  #   - name: Cache cargo registry and tools
  #     uses: actions/cache@v4
  #     with:
  #       path: |
  #         ~/.cargo/bin/
  #         ~/.cargo/registry/
  #         ~/.cargo/git/
  #       key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
  #       restore-keys: ${{ runner.os }}-cargo-registry-
  #   - name: Install and run cargo-deny
  #     run: |
  #       cargo install cargo-deny@0.19.0 --locked || true
  #       cargo-deny -L error --all-features check licenses bans

  # test:
  #   name: Tests
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #   env:
  #     RUSTFLAGS: "-C linker=clang -C link-arg=-fuse-ld=mold"
  #   services:
  #     nats:
  #       image: nats:latest
  #       ports:
  #         - 4222:4222
  #   steps:
  #   - uses: actions/checkout@v4
  #   - name: Install protoc
  #     run: |
  #       PB_REL="https://github.com/protocolbuffers/protobuf/releases"
  #       PROTOC_ZIP="protoc-${PROTOC_VERSION}-linux-x86_64.zip"
  #       curl -fsSLO "$PB_REL/download/v${PROTOC_VERSION}/${PROTOC_ZIP}"
  #       unzip "${PROTOC_ZIP}" -d $HOME/.local
  #       rm "${PROTOC_ZIP}"
  #       echo "$HOME/.local/bin" >> $GITHUB_PATH
  #   - name: Install system dependencies
  #     run: sudo apt-get update -qq && sudo apt-get install -y -qq cmake mold clang
  #   - name: Cache cargo registry
  #     uses: actions/cache@v4
  #     with:
  #       path: |
  #         ~/.cargo/registry/
  #         ~/.cargo/git/
  #       key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
  #       restore-keys: ${{ runner.os }}-cargo-registry-
  #   - name: Verify Cargo.lock is in sync
  #     run: cargo metadata --locked --format-version 1 > /dev/null
  #   - name: Compile and run tests
  #     run: cargo test --locked --all-features --all-targets

  # coverage:
  #   name: Coverage
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #   env:
  #     RUSTFLAGS: "-C linker=clang -C link-arg=-fuse-ld=mold"
  #   services:
  #     nats:
  #       image: nats:latest
  #       ports:
  #         - 4222:4222
  #   steps:
  #   - uses: actions/checkout@v4
  #   - name: Install protoc
  #     run: |
  #       PB_REL="https://github.com/protocolbuffers/protobuf/releases"
  #       PROTOC_ZIP="protoc-${PROTOC_VERSION}-linux-x86_64.zip"
  #       curl -fsSLO "$PB_REL/download/v${PROTOC_VERSION}/${PROTOC_ZIP}"
  #       unzip "${PROTOC_ZIP}" -d $HOME/.local
  #       rm "${PROTOC_ZIP}"
  #       echo "$HOME/.local/bin" >> $GITHUB_PATH
  #   - name: Install system dependencies
  #     run: sudo apt-get update -qq && sudo apt-get install -y -qq cmake mold clang
  #   - name: Cache cargo registry and tools
  #     uses: actions/cache@v4
  #     with:
  #       path: |
  #         ~/.cargo/bin/
  #         ~/.cargo/registry/
  #         ~/.cargo/git/
  #       key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
  #       restore-keys: ${{ runner.os }}-cargo-registry-
  #   - name: Install cargo-llvm-cov
  #     run: cargo install cargo-llvm-cov --locked || true
  #   - name: Add llvm-tools component
  #     run: rustup component add llvm-tools-preview
  #   - name: Generate coverage
  #     run: cargo llvm-cov --all-features --workspace --codecov --output-path codecov.json
  #   - name: Upload to Codecov
  #     uses: codecov/codecov-action@v5
  #     with:
  #       files: codecov.json
  #       fail_ci_if_error: false
  #     env:
  #       CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  # ci-status:
  #   name: CI Status
  #   runs-on: ubuntu-latest
  #   needs: [fmt, clippy, deny, test, coverage]
  #   if: always()
  #   steps:
  #   - name: Check all jobs
  #     run: |
  #       echo '${{ toJson(needs) }}' | jq -e 'to_entries | map(.value.result) | all(. as $result | ["success", "skipped"] | any($result == .))'
